/** Defaults for OBS chapter → trecho window (setup-captura-obs.md §5.1). */
export const DEFAULT_CHAPTER_BEFORE_SEC = 40;
export const DEFAULT_CHAPTER_AFTER_SEC = 20;

/** Waveform peak buckets across full VOD duration. */
export const WAVEFORM_BUCKET_COUNT = 4000;

/** Fixed interval between navigation thumbnails (seconds). */
export const NAV_THUMBNAIL_INTERVAL_SEC = 30;

/** Width of each cached navigation thumbnail (px). */
export const NAV_THUMBNAIL_WIDTH = 160;

/** Minimum Layer-1 selection length (seconds) — technical floor only. */
export const MIN_VOD_SELECTION_SEC = 0.1;
